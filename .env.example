PORT=4173
HOST=127.0.0.1
# Optional AI research. Both values are required to enable calls.
OPENAI_API_KEY=
OPENAI_MODEL=
AI_REQUESTS_PER_HOUR=3
# Create the administrator password in /admin on first local launch.
